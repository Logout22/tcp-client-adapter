- add a --disable-ipv6 flag
- add a --persistent switch to wait for clients indefinitely,
    exit after first connection loss by default
- export libevent's SSL functionality
- allow more than two endpoints (master-slave)?

